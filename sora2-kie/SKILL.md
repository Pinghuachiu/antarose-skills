---
name: sora2-kie
description: Sora2 AI 視頻生成技能，支持 Kie.ai 的 Sora2 和 Sora2 Pro 模型，包括文生視頻、圖生視頻、角色動畫和分鏡視頻生成。
metadata:
  category: ai-generation
  type: video-generation
  languages:
    - python
  platforms:
    - kie.ai
  compatibility:
    - claude-code
    - opencode
    - any-agent
---

# Sora2 KIE - Sora2 AI 視頻生成

使用 Kie.ai 的 Sora2 API 生成高質量 AI 視頻，支持文字轉視頻、圖片轉視頻、角色動畫和分鏡視頻。

## 核心功能

1. **🎬 文生視頻 (Sora2)** - 通過文字描述生成視頻
2. **🖼️ 圖生視頻 (Sora2)** - 將圖片轉換為動態視頻
3. **⚡ Sora2 Pro 文生視頻** - 使用 Pro 模型生成高質量視頻
4. **🎨 Sora2 Pro 圖生視頻** - Pro 模型圖片轉視頻
5. **🎭 角色動畫 (Sora2 Characters)** - 創建動態角色動畫
6. **📋 Sora2 Pro Storyboard** - 從圖像生成分鏡視頻

## 與其他視頻生成 API 的差異

| 特性 | Sora2 KIE | Luma | Kling | Runway |
|------|-----------|------|-------|--------|
| 提供商 | Kie.ai | Luma AI | 快手 | Runway |
| 文生視頻 | ✅ | ✅ | ✅ | ✅ |
| 圖生視頻 | ✅ | ✅ | ✅ | ✅ |
| Pro 版本 | ✅ | ❌ | ❌ | ✅ |
| 角色動畫 | ✅ | ❌ | ❌ | ❌ |
| 分鏡視頻 | ✅ | ❌ | ❌ | ❌ |
| 水印移除 | ✅ | ❌ | ❌ | ✅ |
| API 端點 | 統一端點 | 獨立端點 | 獨立端點 | 獨立端點 |

## 前置要求

### 1. API Key

需要從 Kie.ai 獲取 API Key：
1. 訪問 https://kie.ai/
2. 註冊帳號
3. 前往 API Key 管理頁面獲取 API Key

### 2. 環境變量

設置 `KIE_API_KEY` 環境變量：

```bash
export KIE_API_KEY="your_kie_api_key_here"
```

或在腳本中直接提供：

```bash
python3 scripts/generate.py --api-key "your_api_key" --prompt "..."
```

## 快速開始

### 文生視頻

```bash
python3 .claude/skills/sora2-kie/scripts/generate.py \
  --model sora2 \
  --action text-to-video \
  --prompt "一位教授站在氣氛活躍的教室前排，滿懷熱情地授課"
```

### 圖生視頻

```bash
python3 .claude/skills/sora2-kie/scripts/generate.py \
  --model sora2 \
  --action image-to-video \
  --image-url "https://example.com/image.jpg" \
  --prompt "讓圖片中的元素動起來"
```

### Sora2 Pro 文生視頻

```bash
python3 .claude/skills/sora2-kie/scripts/generate.py \
  --model sora2-pro \
  --action text-to-video \
  --prompt "一隻可愛的橘色斑紋貓在陽光下打哈欠"
```

### 角色動畫

```bash
python3 .claude/skills/sora2-kie/scripts/generate.py \
  --model sora2 \
  --action characters \
  --video-url "https://example.com/character.mp4" \
  --character-prompt "一個友好的卡通角色，富有表現力的眼睛"
```

### 分鏡視頻

```bash
python3 .claude/skills/sora2-kie/scripts/generate.py \
  --model sora2-pro \
  --action storyboard \
  --image-urls "https://example.com/img1.jpg,https://example.com/img2.jpg" \
  --scenes "場景1: 場景描述1,場景2: 場景描述2"
```

### 查詢任務狀態

```bash
python3 .claude/skills/sora2-kie/scripts/generate.py \
  --action status \
  --task-id "task_id_here"
```

## 模型說明

### Sora2 模型

**文生視頻 (sora-2-text-to-video)**
- 基礎文字轉視頻模型
- 支持多種寬高比
- 可選水印移除
- 生成幀數：5-10 秒

**圖生視頻 (sora-2-image-to-video)**
- 將靜態圖片轉換為動態視頻
- 保持圖片視覺風格
- 自動生成動態效果

**角色動畫 (sora-2-characters)**
- 上傳角色視頻（1-4 秒，10MB 以內）
- 生成動態角色動畫
- 支持自定義角色描述和安全指令

### Sora2 Pro 模型

**Pro 文生視頻 (sora-2-pro-text-to-video)**
- 更高質量的視頻生成
- 更好的動態一致性
- 更精確的提示詞遵循

**Pro 圖生視頻 (sora-2-pro-image-to-video)**
- Pro 級別圖片轉視頻
- 更流暢的動畫效果
- 更好的細節保留

**Pro Storyboard (sora-2-pro-storyboard)**
- 從多張圖片生成分鏡視頻
- 支持多場景描述
- 自動場景過渡

## 參數說明

### 通用參數

| 參數 | 說明 | 必需 |
|------|------|------|
| `--model` | 模型選擇 (sora2/sora2-pro) | ✅ |
| `--action` | 執行動作 | ✅ |
| `--api-key` | Kie.ai API Key | ❌ (可從環境變量讀取) |

### 文生視頻參數

| 參數 | 說明 | 必需 |
|------|------|------|
| `--prompt` | 文字描述 (最多 5000 字符) | ✅ |
| `--aspect-ratio` | 寬高比 (landscape/portrait/square) | ❌ |
| `--frames` | 生成幀數 | ❌ |
| `--remove-watermark` | 移除水印 (true/false) | ❌ |
| `--callback-url` | 回調 URL | ❌ |

### 圖生視頻參數

| 參數 | 說明 | 必需 |
|------|------|------|
| `--image-url` | 圖片 URL | ✅ |
| `--prompt` | 文字描述 | ❌ |
| `--aspect-ratio` | 寬高比 | ❌ |
| `--frames` | 生成幀數 | ❌ |

### 角色動畫參數

| 參數 | 說明 | 必需 |
|------|------|------|
| `--video-url` | 角色視頻 URL (1-4秒, <10MB) | ✅ |
| `--character-prompt` | 角色描述 (最多 5000 字符) | ❌ |
| `--safety-instruction` | 安全指令 (最多 5000 字符) | ❌ |

### 分鏡視頻參數

| 參數 | 說明 | 必需 |
|------|------|------|
| `--image-urls` | 圖片 URL（逗號分隔） | ✅ |
| `--scenes` | 場景描述（JSON 格式或逗號分隔） | ✅ |
| `--aspect-ratio` | 寬高比 | ❌ |
| `--frames` | 每場景幀數 | ❌ |

## 寬高比選項

- `landscape` - 橫向 (16:9)
- `portrait` - 縱向 (9:16)
- `square` - 正方形 (1:1)

## 幀數選項

- `5` - 5 秒視頻
- `10` - 10 秒視頻
- `15` - 15 秒視頻

## API 端點

所有 Sora2 模型使用統一的 API 端點：

```
POST https://api.kie.ai/api/v1/jobs/createTask
```

查詢任務狀態：

```
GET https://api.kie.ai/api/v1/jobs/getTaskDetails?taskId={task_id}
```

## 使用示例

### 示例 1：生成簡單文生視頻

```bash
python3 scripts/generate.py \
  --model sora2 \
  --action text-to-video \
  --prompt "一隻金毛尋回犬在海灘上奔跑，陽光灑落下來" \
  --aspect-ratio landscape \
  --frames 10 \
  --remove-watermark true
```

### 示例 2：使用 Pro 模型生成高質量視頻

```bash
python3 scripts/generate.py \
  --model sora2-pro \
  --action text-to-video \
  --prompt "宏偉的科幻城市夜景，霓虹燈閃爍，飛行汽車穿梭" \
  --frames 10
```

### 示例 3：圖片轉視頻

```bash
python3 scripts/generate.py \
  --model sora2 \
  --action image-to-video \
  --image-url "https://example.com/sunset.jpg" \
  --prompt "讓雲彩緩慢移動，太陽逐漸落下" \
  --frames 10
```

### 示例 4：創建角色動畫

```bash
# 首先上傳角色視頻到可公開訪問的 URL
python3 scripts/generate.py \
  --model sora2 \
  --action characters \
  --video-url "https://example.com/character.mp4" \
  --character-prompt "一個友好的卡通角色，有著富有表現力的眼睛和流暢的動作" \
  --safety-instruction "確保動畫適合家庭觀看"
```

### 示例 5：生成分鏡視頻

```bash
python3 scripts/generate.py \
  --model sora2-pro \
  --action storyboard \
  --image-urls "img1.jpg,img2.jpg,img3.jpg" \
  --scenes "場景1: 日出時的湖面,場景2: 白天的森林,場景3: 星空下的營地" \
  --frames 15
```

## 輪詢任務狀態

由於視頻生成需要時間，建議使用輪詢檢查任務狀態：

```bash
# 提交任務
python3 scripts/generate.py \
  --model sora2 \
  --action text-to-video \
  --prompt "測試視頻" \
  --save-task-id

# 等待一段時間後查詢狀態
python3 scripts/generate.py \
  --action status \
  --task-id $(cat task_id.txt)
```

或使用自動輪詢：

```bash
python3 scripts/generate.py \
  --model sora2 \
  --action text-to-video \
  --prompt "測試視頻" \
  --poll \
  --poll-interval 10
```

## 輸出格式

成功任務返回：

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "task_sora-2-text-to-video_xxx",
    "state": "success",
    "resultJson": {
      "video_url": "https://...",
      "duration": 5,
      "width": 1920,
      "height": 1080
    }
  }
}
```

## 錯誤處理

常見錯誤代碼：

- `401` - 未授權（API Key 無效）
- `402` - 積分不足
- `404` - 任務不存在
- `422` - 參數驗證失敗
- `429` - 請求頻率過高
- `500` - 服務器錯誤
- `501` - 生成失敗

## 費用說明

Kie.ai 使用積分系統：
- 不同模型消耗不同積分
- Sora2 Pro 消耗更多積分
- 詳情請查看 https://kie.ai/pricing

## 限制與規範

### 文字限制

| 參數 | 最大長度 |
|------|---------|
| prompt | 5000 字符 |
| character_prompt | 5000 字符 |
| safety_instruction | 5000 字符 |

### 文件限制

| 類型 | 限制 |
|------|------|
| 角色視頻時長 | 1-4 秒 |
| 角色視頻大小 | < 10MB |
| 視頻格式 | MP4, WebM, AVI |

### 視頻規格

| 項目 | 說明 |
|------|------|
| 最大分辨率 | 1080p |
| 視頻長度 | 5-15 秒 |
| 支援格式 | MP4 |

## 內容政策與限制

### 🚫 重要內容限制

根據 OpenAI 的 Sora2 內容政策，以下類型的內容會被**拒絕生成**：

#### 1. 圖片上傳限制

**❌ 不允許上傳的圖片類型：**
- 包含**真人照片**的圖片（photorealistic people）
- 明顯的可識別人物肖像
- 真實人物的臉部特寫

**錯誤訊息示例：**
```
Error code: 400
Error message: OpenAI currently do not support uploads of images containing photorealistic people.
```

**✅ 允許的圖片類型：**
- 動畫角色
- 卡通風格插畫
- 抽象藝術
- 風景靜物
- 3D 渲染角色（非真人）
- 手繪插畫
- 數位藝術作品

#### 2. 角色動畫限制

使用 `sora-2-characters` 模型時：
- 上傳的角色視頻應為**非真人**角色
- 建議使用動畫、卡通、或 3D 角色模型
- 避免使用真實人物視頻片段

#### 3. 內容安全規範

**禁止生成的內容：**
- 暴力、血腥、恐怖內容
- 色情、露骨成人內容
- 仇恨言論、歧視性內容
- 危險活動、非法行為
- 侵犯版權或商標的內容
- 詐騙、欺詐相關內容

### 💡 避免被拒絕的建議

#### 提示詞撰寫技巧

**✅ 好的提示詞示例：**
```
一位動畫風格的女僕角色在可愛的房間裡開心地揮手，
背景有溫馨的裝飾，角色有著大大的眼睛和豐富的表情
```

**❌ 容易被拒絕的提示詞：**
```
真實的女僕穿著制服...（可能被識別為真人）
某位知名藝人的樣子...（侵犯肖像權）
```

#### 圖片選擇建議

1. **使用動畫風格圖片**
   - 動漫、卡通風格
   - 手繪風格
   - 3D 渲染風格

2. **避免圖片類型**
   - 照片級真實照片
   - 包含真實人物的新聞圖片
   - 明顯的人物肖像

3. **圖片內容建議**
   - 使用原創角色設計
   - 選擇非人物為主體的場景
   - 使用抽象或藝術化風格

### 常見錯誤場景與解決方案

| 錯誤場景 | 錯誤代碼 | 解決方案 |
|---------|---------|---------|
| 上傳包含真人的圖片 | 400 | 更換為動畫/卡通風格圖片 |
| 使用真實人物肖像 | 400 | 使用原創或虛構角色 |
| 提示詞包含暴力描述 | 400 | 修改為友善、安全的描述 |
| 上傳包含商標的圖片 | 400 | 移除或遮蔽商標元素 |
| 角色視頻包含真人 | 400 | 使用動畫角色或 3D 模型 |

### 實際案例

**案例 1：真實圖片被拒絕**
```bash
# ❌ 錯誤操作
python3 scripts/generate.py \
  --model sora2 \
  --action image-to-video \
  --image-url "https://example.com/real-person-photo.jpg" \
  --prompt "讓這個人動起來"

# 錯誤訊息：OpenAI currently do not support uploads of images containing photorealistic people

# ✅ 正確操作
python3 scripts/generate.py \
  --model sora2 \
  --action image-to-video \
  --image-url "https://example.com/anime-character.png" \
  --prompt "讓動畫角色優雅地旋轉"
```

**案例 2：使用安全指令**
```bash
# ✅ 使用 safety-instruction 參數確保內容安全
python3 scripts/generate.py \
  --model sora2 \
  --action characters \
  --video-url "https://example.com/cartoon-character.mp4" \
  --character-prompt "一個友善的卡通角色" \
  --safety-instruction "確保動畫適合所有年齡層觀看，避免任何暴力或恐怖元素"
```

### 內容審查流程

Sora2 使用自動化內容審查系統：
1. **提交時檢查** - 立即拒絕明顯違規內容
2. **生成時監控** - 實時監控生成過程
3. **結果驗證** - 最終輸出前的安全檢查

**重要提示：** 即使提示詞通過初始檢查，生成過程中仍可能因為安全政策而中斷或失敗。

### 申訴與問題回報

如果您認為內容被錯誤拒絕：
1. 檢查是否違反上述政策
2. 確認圖片或提示詞不包含敏感內容
3. 聯繫 Kie.ai 客服申訴
4. 提供詳細的錯誤訊息和使用場景

## 注意事項

1. **API Key 安全**
   - 不要將 API Key 提交到版本控制
   - 定期更換 API Key
   - 使用環境變量存儲

2. **視頻生成時間**
   - 文生視頻通常需要 2-5 分鐘
   - 圖生視頻通常需要 1-3 分鐘
   - Pro 模型可能需要更長時間

3. **積分管理**
   - 定期檢查積分餘額
   - 避免積分不足導致任務失敗

4. **內容規範**
   - 遵守服務條款
   - 避免生成違規內容
   - 使用安全指令過濾不當內容
   - **特別注意：不要上傳包含真人的圖片**

## 與其他技能整合

### social-content-writer

生成視頻後發布到社交平台：

```bash
# 1. 生成視頻
python3 .claude/skills/sora2-kie/scripts/generate.py \
  --model sora2 \
  --action text-to-video \
  --prompt "產品展示視頻" \
  --poll

# 2. 發布到社交平台
python3 .claude/skills/social-content-writer/scripts/publish.py \
  --platform tiktok \
  --video "generated_video.mp4"
```

## 授權

MIT License
